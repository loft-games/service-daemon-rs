[package]
name = "example-diagnostics"
version = "0.1.0"
edition = "2021"
publish = false
license = "MIT OR Apache-2.0"

[dependencies]
service-daemon = { path = "../../service-daemon", features = ["diagnostics"] }
tokio = { version = "1.53.0", features = ["full"] }
anyhow = "1.0.104"
tracing = "0.1.44"
uuid = "1.24.0"
