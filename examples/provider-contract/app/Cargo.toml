[package]
name = "example-provider-contract"
version = "0.1.0"
edition = "2024"
publish = false
license = "MIT OR Apache-2.0"
description = "Provider contract example: shared type with app-local fallback implementations."

[dependencies]
anyhow = "1.0.104"
example-provider-contract-shared = { path = "../shared" }
service-daemon = { path = "../../../service-daemon" }
tokio = { version = "1.53.0", features = ["full"] }
tracing = "0.1.44"
